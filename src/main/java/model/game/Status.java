package model.game;

/**
 * The enum Status.
 */
public enum Status {
    /**
     * Continue game status.
     */
    CONTINUE_GAME,
    /**
     * Skip move status.
     */
    SKIP_MOVE,
    /**
     * Level up status.
     */
    LEVEL_UP,
    /**
     * Restart game status.
     */
    RESTART_LEVEL,
    /**
     * Game over status.
     */
    GAME_OVER
}
